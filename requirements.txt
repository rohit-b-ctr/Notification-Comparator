flask~=3.1
psycopg2-binary~=2.9
sshtunnel~=0.4
paramiko==2.12.0        # pinned — newer versions break sshtunnel key parsing
deepdiff~=9.1
jinja2~=3.1             # also a Flask transitive dep; explicit for clarity
websocket-client~=1.9
PyMuPDF~=1.26
xmltodict~=1.0
requests~=2.31          # used for Kowl REST API fetches
cryptography~=48.0      # encrypts secrets (DB pass, SSH key) at rest in config.json

# Optional (NOT a pip package) — for Allure HTML report generation in Full Run:
#   brew install allure   (needs a JRE)   OR   npm i -g allure-commandline
# Without it, Full Run still emits a downloadable allure-results .zip.
